<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liquid.kochanowski" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".KochanowskiMainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SyncActivity"
            android:configChanges="screenSize|orientation"
            android:label="@string/title_activity_sync"
            android:parentActivityName=".KochanowskiMainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.liquid.kochanowski.KochanowskiMainActivity" />
        </activity>
        <activity
            android:name=".TimeTableTabActivity"
            android:label="@string/title_activity_time_table_tab"
            android:parentActivityName=".KochanowskiMainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.liquid.kochanowski.KochanowskiMainActivity" />
        </activity>
    </application>

</manifest>
